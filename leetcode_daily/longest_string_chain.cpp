#include<bits/stdc++.h>
using namespace std;
/*
    ok before calculating if words[i-1].length > words[i].length then no need to check if words[i-1] is predeccer

    //may be suffix tree
*/

class Solution {
public:
    int longestStrChain(vector<string>& words) {
        
    }
};
int main(){
    
}