#include<bits/stdc++.h>
using namespace std;


//brute force 
// map of 0 to n and then iterate over elements

//optimisez 
//if we have 0 to n elements and xor with all elements then last remiaing element is ans

class Solution {
public:
    int missingNumber(vector<int>& nums) {
        //we have 0 to n and 1 is missing means size of array is n (0th is mizsing)
        int n=nums.size()+1;
        int i=0;
        int ans=0;
        for(int i=0;i<nums.size();i++){
        
        }
        
    }
};