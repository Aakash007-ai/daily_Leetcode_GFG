#include<bits/stdc++.h>
using namespace std;

// if you don't get to perfect solution but you have a this solution can be optimized 
// (think while coding your optimized solution)

class Solution {
public:
    // int maxProfit(vector<int>& prices) {
    //     int n= prices.size();
    //     int ans = 0;
    //     int prevMin = prices[0];
    //     for(int i=1;i<n;i++){
    //         prevMin = min(prevMin, prices[i-1]);
    //         ans = max(ans,)
    //     }     
    // }
};