/*
 * public means this class is accessible over all project 
 * private means accessible only within the declared class itself
 * protected provide same access as the default access modifier with addition that subclasses can access protected methods
 * and variable of the super class
 */
public class Dog {
    void bark (){
        System.out.println("Dog bark");
    }
}
