// k sorted array
//  assume a sorted array of that array
// the elements in sorted elements present in range of i-k,i+k

// make a k sorted array
/*
    we make a min priority of size k,
    and start traversing and pusing in heap
    once size of heap> k insert in res array

    in that way the resultant array is ksorted array
*/