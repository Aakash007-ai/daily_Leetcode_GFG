#include<bits/stdc++.h>
using namespace std;


int main(){
    int n;
    cin>>n;

    unordered_map<int,int> mp;


    // map testing ------ so if we just write mp[0]  map will insert that key with value 0;
    // if(mp.find(0) == mp.end()){
    //     cout<<"0 key is not there in map\n";
    //     cout<<mp[0];
    // };
    // if(mp.find(0) != mp.end()){
    //     cout<<"\n0 is there in map by just declaring mp[0]";
    // }
    // cout<<mp[0]; //if it is not there it fill map with key and value as 0

    

    //initially elements in array is 0 if we declare with size
    // vector<int> arr(n); //we have create vector of size n
    // cout<<arr[0];//initially array will be filled with 0

    //initially elements in array is what we declare with size
    // vector<int> arr(n,-1); //we have create vector of size n
    // cout<<arr[0];//initially array will be filled with -1

    //printing an empty integer will give us 0
    // int testing;
    // cout<<testing;
}