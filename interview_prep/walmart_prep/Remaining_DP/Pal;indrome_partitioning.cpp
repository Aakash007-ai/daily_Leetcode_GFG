#include<bits/stdc++.h>
using namespace std;

//find palindroim partitiong

//nitin is completely a palindrome

//at most size -1 partition

//so in order to partioitn a single char string we need 0  partition

//so we have to partiont such that we have minimum number of partitions



//find i and j 

int solve(int string,int i,int j ){
    //base condition
    if(i>j) return 0;

    //but in our case we have to return if there is just one character


}