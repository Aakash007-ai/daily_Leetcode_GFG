#include<bits/stdc++.h>
using namespace std;

class Solution
{
    public:
    
    // Function to reverse first k elements of a queue.
    queue<int> modifyQueue(queue<int> q, int k) {
        // add code here.
        stack<int> st;
        while(k--){
            //first put all elements in stack then , push back it in queue
            // now dequeue from  front and push back size -k times
        }
    }
};