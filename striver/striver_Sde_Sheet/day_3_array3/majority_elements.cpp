#include<bits/stdc++.h>
using namespace std;

/**
 * majority elements that appear more than n/2 times
 * iterate over elements and take a constant variable x (as a map, if a elements become 0 replace it else increment if equal else decrement)
 */