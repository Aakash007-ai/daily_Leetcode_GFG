#include<bits/stdc++.h>
using namespace std;

class Solution {
  public:
    // Function to detect cycle in a directed graph.
    bool isCyclic(int V, vector<int> adj[]) {
        //we just check if indeg of all become 0 or not
        //at last only that element indeg will be which is cyclic
    }
};