internal implementation of hashMap

so we do 
Map hashMap = new HashMap<>();

by default there is no size we provide, by default it create a map of size 16
and size of hash map is in the power of 2

we generate hascode and put againt that hashnode

hashcode generated is of length 